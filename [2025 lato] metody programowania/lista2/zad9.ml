
(* merge sort *)

let split xs = 
  let rec split_acc xs acc1 acc2 = 
    match xs with
    | [] -> 


let rec merge xs ys = 
  match (xs, ys) with
  | ([], _) -> ys
  | (_, []) -> xs
  | 


let rec merge_sort xs
